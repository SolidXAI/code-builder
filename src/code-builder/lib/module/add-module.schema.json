{
  "properties": {
    "module": {
      "type": "string",
      "description": "The name of the module"
    },
    "generateChecksum": {
      "type": "boolean",
      "description": "Whether to generate a checksum for the module",
      "default": false
    }
  },
  "required": ["module"]
}
