{
  "properties": {
    "module": {
      "type": "string",
      "description": "The name of the existing module"
    },
    "model": {
      "type": "string",
      "description": "The name of the model within the module"
    }
  },
  "required": [
    "module",
    "model"
  ]
}
