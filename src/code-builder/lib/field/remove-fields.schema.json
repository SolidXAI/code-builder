{
  "properties": {
    "module": {
      "type": "string",
      "description": "The name of the existing module"
    },
    "model": {
      "type": "string",
      "description": "The name of the existing model"
    },
    "fieldNames": {
      "oneOf": [
        {
          "type": "array",
          "items": {
            "type": "string",
            "description": "Name of a field to remove"
          },
          "description": "An array of field names to remove"
        },
        {
          "type": "string",
          "description": "Name of a field to remove"
        }
      ],
      "description": "The field name(s) to remove from the model"
    }
  },
  "required": ["module", "model", "fieldNames"]
}
