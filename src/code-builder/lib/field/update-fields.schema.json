{
  "properties": {
    "module": {
      "type": "string",
      "description": "The name of the existing module"
    },
    "model": {
      "type": "string",
      "description": "The name of the existing model"
    },
    "fields": {
      "oneOf": [
        {
          "type": "array",
          "items": {
            "type": "string",
            "description": "JSON stringfied Metadata about a field"
          },
          "description": "An array of field metadata"
        },
        {
          "type": "string",
          "description": "JSON stringfied Metadata about a field"
        }
      ],
      "description": "The field metadata for fields for the model"
    }
  },
  "required": ["module", "model"]
}